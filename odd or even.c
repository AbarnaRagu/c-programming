#include <stdio.h>
int main() {
    int num;
    printf("enter the number");
    scanf("%d",&num);
    if(num%2==0)
    
       printf("\n the  number is even");
     else
        printf ("\n the number is odd");
    return 0;
}
